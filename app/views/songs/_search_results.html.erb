<% @results["response"]["songs"].each do |song| %>
  <tr>
    <td><%= song["title"] %></td> 
    <td><%= song["artist_name"] %></td>
    <td><%= render 'playlist_selector', song: song %></td>
  </tr>
<% end %>